import { shallowMount } from "@vue/test-utils";
import AppCard from "@/components/AppCard.vue";

xdescribe("AppCard.vue", () => {
  it("", () => {
    const wrapper = shallowMount(AppCard, {});
  });
});
